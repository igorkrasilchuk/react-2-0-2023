const baseURL = 'https://jsonplaceholder.typicode.com'

const urls = {
    posts: '/posts',
    users: '/users',
    todos: '/todos',
    comments: '/comments',
    albums: '/albums',
    postId: ':postId',
}

export {
    baseURL,
    urls
}